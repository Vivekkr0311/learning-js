function add(param1, param2){
    return param1 + param2;
}
var res = add(1, 2 /*Arguments8*/);
console.log(res);

//The values which are passed inside the function are called "Arguments".
//The labels in function which gets those arguments are called "Parameters".